<h1>Task: <%= @task.id %> の編集ページ</h1>

<%= form_with(model: @task, local:true) do |f| %>
  <%= f.label :content, 'To Do' %>
  <%= f.text_field :content %>
  
<%= f.submit '追加' %>
<% end %>

<%= link_to 'タスク一覧へ戻る', tasks_path %>