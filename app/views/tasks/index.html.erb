<h1>タスクリスト</h1>

<ul>
    <% @tasks.each do |task| %>
    <li><%= link_to task.id, task %>:<%= task.status %> &gt; <%= task.content %></li>
    <% end %>
</ul>
<%= paginate @tasks %>

<%= link_to 'タスク新規追加', new_task_path %>