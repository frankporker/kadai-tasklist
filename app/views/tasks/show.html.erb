<h1>Task: <%= @task.id %> のページ</h1>

<table class="table table-bordered">
    <tr>
        <th>id</th>
        <td><%= @task.id %></td>
    </tr>
    <tr>
        <th>title</th>
        <td><%= @task.status %></td>
    </tr>
    <tr>
        <th>task</th>
        <td><%= @task.content %></td>
    </tr>
</table>
<p>Title: <%= @task.status %></p>
<p>Task: <%= @task.content %></p>

<%= link_to 'このタスク内容を編集する', edit_task_path(@task), class: 'btn btn-light' %>
<%= link_to 'このタスク内容を削除する',@task, method: :delete, data: {confirm: '本当に削除しても宜しいでしょうか？'}, class:'btn btn-danger' %>